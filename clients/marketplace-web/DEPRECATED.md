# Deprecated

Public marketplace UI ships inside `clients/web` (`/marketplace` and
`/{org}/marketplace`). Do not deploy or rebuild this package.

Oilan routes `market.l8ai.cn` → `https://agents.l8ai.cn/marketplace`.
