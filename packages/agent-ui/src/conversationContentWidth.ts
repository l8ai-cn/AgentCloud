// Single source for the conversation reading column. Every stacked band —
// timeline, empty state, approval dock, status strip, composer — must share it,
// otherwise the composer and the messages above it center on different axes.
export const CONVERSATION_CONTENT_WIDTH = "mx-auto w-full max-w-3xl";
